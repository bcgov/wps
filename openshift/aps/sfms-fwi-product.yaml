kind: Product
name: SFMS Daily Fire Weather Index${DISPLAY_SUFFIX}
dataset: psu-sfms-daily-fwi-${SUFFIX}
environments:
  - name: ${ENV_TIER}
    active: true
    approval: true
    flow: kong-api-key-acl
    services: [psu-sfms-fwi-${SUFFIX}]
