
if [[ (! -z "$APP_USER") &&  (! -z "$APP_PASSWORD") && (! -z "$APP_DATABASE")]]; then
    echo "Loading PostGIS extensions into $APP_DATABASE"
    echo "\c ${APP_DATABASE} \\ CREATE EXTENSION IF NOT EXISTS postgis;" | psql "$1" -w
fi
