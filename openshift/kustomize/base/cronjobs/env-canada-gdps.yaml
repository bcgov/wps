apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app: wps-__SUFFIX__
    app.kubernetes.io/part-of: wps
    wps.bcgov/env-global: 'true'
    wps.bcgov/env-uv: 'true'
    wps.bcgov/env-postgres: 'true'
    wps.bcgov/env-redis: 'true'
    wps.bcgov/env-wfwx: 'true'
    wps.bcgov/env-redis-dailies: 'true'
    wps.bcgov/env-redis-cache-env-canada: 'true'
    wps.bcgov/env-data-retention: 'true'
  name: env-canada-gdps-wps-__SUFFIX__
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 5
  jobTemplate:
    metadata:
      labels:
        app: wps-__SUFFIX__
        cronjob: env-canada-gdps-wps-__SUFFIX__
    spec:
      backoffLimit: 6
      template:
        spec:
          containers:
          - command:
            - uv
            - run
            - --no-sync
            - python
            - -m
            - weather_model_jobs.env_canada
            - GDPS
            env:
            - name: WFWX_MAX_PAGE_SIZE
              value: '1000'
            image: image-registry.openshift-image-registry.svc:5000/e1e498-tools/wps-jobs-__SUFFIX__:__SUFFIX__
            imagePullPolicy: Always
            name: env-canada-gdps-wps-__SUFFIX__
            resources:
              limits:
                memory: 1Gi
              requests:
                cpu: 500m
                memory: 512Mi
          restartPolicy: OnFailure
  schedule: 9 * * * *
  successfulJobsHistoryLimit: 3
