{
  "version": 1,
  "formatters": {
    "simple": {
      "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    }
  },
  "handlers": {
    "console": {
      "class": "logging.StreamHandler",
      "level": "DEBUG",
      "formatter": "simple",
      "stream": "ext://sys.stdout"
    }
  },
  "loggers": {
    "forecasts": {
      "level": "DEBUG",
      "handlers": ["console"],
      "propagate": false
    },
    "auth": {
      "level": "DEBUG",
      "handlers": ["console"],
      "propagate": false
    },
    "wildfire_one": {
      "level": "DEBUG",
      "handlers": ["console"],
      "propagate": false
    },
    "config": {
      "level": "DEBUG",
      "handlers": ["console"],
      "propagate": false
    },
    "tests": {
      "level": "DEBUG",
      "handlers": ["console"],
      "propagate": false
    }
  },
  "root": {
    "level": "INFO",
    "handlers": ["console"]
  }
}
