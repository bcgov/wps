name: Manual

on:
  workflow_dispatch:
    inputs:
      pr:
        description: "Pull request #"
        required: true
jobs:
  do_something:
    runs-on: ubuntu-20.04
    steps:
      - name: Some step
      - run: |
          echo "PR ${{ github.event.inputs.pr }}"
