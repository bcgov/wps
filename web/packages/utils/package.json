{
  "name": "@wps/utils",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./*": "./dist/*.js"
  },
  "typesVersions": {
    "*": { "*": ["./src/*"] }
  },
  "devDependencies": {
    "@wps/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "coverage:ci": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  }
}
