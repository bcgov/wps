{
  "name": "@wps/api",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*.js"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "coverage:ci": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  }
}
