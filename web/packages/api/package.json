{
  "name": "@wps/api",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./*": "./dist/*.js"
  },
  "typesVersions": {
    "*": { "*": ["./src/*"] }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "coverage:ci": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@wps/utils": "workspace:*"
  }
}
